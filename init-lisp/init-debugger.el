;;; init-debugger.el --- Some debugger
;; -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package which-key
  :demand t
  :config
  (which-key-mode))

(provide 'init-debugger)
;;; init-debugger.el ends here
